module.exports = {
  dependencies: {
    // Autolinking enabled for all platforms.
    // If you previously saw "Cannot resolve symbol ReactNativeFirebaseAppPackage",
    // run: npx expo prebuild --clean && npx expo run:android
  },
};
